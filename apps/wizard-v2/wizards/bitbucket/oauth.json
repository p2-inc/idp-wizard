{
  "schemaVersion": "1.0",
  "id": "bitbucket-oauth",
  "providerId": "bitbucket",
  "protocol": "oauth",
  "title": "Bitbucket OAuth",
  "alias": {
    "prefix": "bitbucket-oauth",
    "sessionKey": "p2_bitbucket_oauth"
  },
  "idpConfig": {
    "providerId": "bitbucket",
    "hideOnLogin": true,
    "defaults": {
      "syncMode": "FORCE"
    }
  },
  "steps": [
    {
      "id": 1,
      "title": "Create an OAuth Consumer",
      "blocks": [
        {
          "type": "text",
          "content": "In Bitbucket, go to your workspace settings → OAuth consumers and click 'Add consumer'. Give it a name and enable the 'Email' and 'Account' permissions. Use the callback URL below."
        },
        {
          "type": "copy",
          "label": "Callback URL",
          "value": "{{api.ssoUrl}}",
          "hint": "Paste this as the Callback URL in Bitbucket"
        }
      ]
    },
    {
      "id": 2,
      "title": "Client Credentials",
      "enableNextWhen": "state.credentialsProvided",
      "blocks": [
        {
          "type": "text",
          "content": "After saving the consumer, copy the Key and Secret and paste them below."
        },
        {
          "type": "formGroup",
          "id": "bitbucketCreds",
          "exclusive": false,
          "forms": [
            "clientCredentials"
          ]
        }
      ]
    },
    {
      "id": 3,
      "title": "Confirmation",
      "type": "confirm",
      "blocks": [
        {
          "type": "confirm",
          "title": "Create Bitbucket Identity Provider",
          "description": "Configuration is complete. Click the button below to create the identity provider in Keycloak.",
          "buttonText": "Create Bitbucket Identity Provider",
          "action": "createIdp",
          "adminLink": "{{api.adminLinkSocial}}",
          "adminButtonText": "Manage Bitbucket Identity Provider"
        }
      ]
    }
  ],
  "forms": {
    "clientCredentials": {
      "title": "Bitbucket OAuth Credentials",
      "description": "Enter the Key and Secret from your Bitbucket OAuth consumer.",
      "fields": [
        {
          "id": "clientId",
          "type": "text",
          "label": "Key",
          "placeholder": "your-consumer-key",
          "required": true
        },
        {
          "id": "clientSecret",
          "type": "password",
          "label": "Secret",
          "placeholder": "your-consumer-secret",
          "required": true
        }
      ],
      "submit": {
        "label": "Save Credentials",
        "action": "saveCredentials"
      }
    }
  },
  "actions": {
    "saveCredentials": {
      "type": "saveForm",
      "fields": ["clientId", "clientSecret"],
      "dispatch": ["CREDENTIALS_PROVIDED"]
    },
    "createIdp": {
      "endpoint": "createIdp",
      "method": "POST",
      "contentType": "json",
      "body": {
        "alias": "{{alias}}",
        "displayName": "{{alias}}",
        "providerId": "bitbucket",
        "hideOnLogin": true,
        "config": {
          "syncMode": "FORCE",
          "clientId": "{{state.formValues.clientId}}",
          "clientSecret": "{{state.formValues.clientSecret}}"
        }
      },
      "onSuccess": {
        "dispatch": [
          {
            "type": "SUBMIT_SUCCESS",
            "result": "Bitbucket Identity Provider created successfully."
          }
        ],
        "then": [
          "clearAlias"
        ]
      },
      "messages": {
        "error": "Error creating Identity Provider. One may already be configured for this realm."
      }
    },
    "clearAlias": {
      "type": "clearAlias"
    }
  }
}
